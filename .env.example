# Copy this file to .env and set MODELS_DIR to the absolute host path
# where your model directories live (each model as its own subfolder,
# e.g. MODELS_DIR/Huihui-Qwen3.6-35B-A3B-abliterated-w8a8).
MODELS_DIR=/path/to/Models
